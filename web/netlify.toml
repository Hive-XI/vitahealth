# Picked up after Netlify cds into web/ (base directory).
# Redirects here plus public/_redirects cover SPA routing.

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
